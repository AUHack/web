{
  "name": "auhack-web",
  "private": true,
  "version": "0.0.1",
  "description": "Website for AUHack",
  "author": "Nicklas Vedsted",
  "scripts": {
    "clean": "rm -rf build",
    "collectstatic": "cp -r static/ build",
    "start": "yarn clean; yarn collectstatic; pug --watch --out=build --basedir=includes/ pages",
    "build": "yarn clean; yarn collectstatic; pug --out=build --basedir=includes/ pages"
  },
  "dependencies": {
    "pug-cli": "^1.0.0-alpha6"
  },
  "repository": {
    "url": "https://github.com/AUHack/web",
    "type": "git"
  }
}
